steps to run:

FIRST TIME ONLY:
1.) go to https://dashboard.ngrok.com/ and make an account
2.) on the dashboard page look at step 2 and copy the token (should look like the bunch of letters and numbers) ignore the rest
3.) copy the token into token.txt
4.) right click add_token and run with powershell

EVERY TIME:
5.) start your minecraft open to lan world
6.) copy the port in to port.txt
7.) right click start_server and run 
8.) in the window copy the url after tcp (sor for url=tcp://0.tcp.ngrok.io:14679 copy 0.tcp.ngrok.io:14679 and give to friends)
9.) enjoy